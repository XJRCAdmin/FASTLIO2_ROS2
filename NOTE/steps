# 1. 加载 can-dev 内核模块（如果未加载）
sudo modprobe can
sudo modprobe can_raw

# 2. 配置 CAN 接口（以 can0 为例，波特率 1 Mbps）
sudo ip link set can0 down
sudo ip link set can0 type can bitrate 1000000
sudo ip link set can0 up

# 3. 验证状态
ip -details link show can0

正确输出：
can0: <NOARP,UP,LOWER_UP,ECHO> mtu 16 qdisc pfifo_fast state UP ...
    link/can  promiscuity 0
    can <BERR-REPORTING> state ERROR-ACTIVE restart-ms 0 
    bitrate 1000000 sample-point 0.875 


//或者虚拟can：
sudo modprobe vcan
sudo ip link add dev vcan0 type vcan
sudo ip link set up vcan0

source ./install/setup.bash 
ros2 launch rc_serial_driver serial_driver.launch.py

sudo apt install can-utils
candump can0